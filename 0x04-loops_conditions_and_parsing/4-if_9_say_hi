#!/usr/bin/env bash
# Displays Best School 10 times with Hi on 9th iteration

count=1
while [[ "$count" -le 10 ]]
do
	echo "Best School"
	count=$((count + 1))
	if [ $count == 10 ]; then
		echo "Hi"
	fi
done
